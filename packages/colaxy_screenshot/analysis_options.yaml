include: ../../analysis_options.yaml

analyzer:
  errors:
    # This package writes files and drives a desktop window; the capture loop
    # reports progress on the console by design.
    avoid_print: ignore
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
